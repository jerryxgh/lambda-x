;;; lambda-lua.el --- Lua script

;;; Commentary:

;;; Code:

(require 'lambda-core)

;; lua-mode -------------------------------------------------------------------
(use-package lua-mode
  :ensure t)

(provide 'lambda-lua)

;;; lambda-lua.el ends here

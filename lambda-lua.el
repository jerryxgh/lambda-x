;;; lambda-lua.el --- Lua script

;;; Commentary:

;;; Code:

(require 'lambda-core)

;; lua-mode -------------------------------------------------------------------
(lambda-package-ensure-install 'lua-mode)

(provide 'lambda-lua)

;;; lambda-lua.el ends here

# -*- mode: snippet -*-
# name: template
# key: template
# contributor: gh_xu@qq.com
# --
;;; `(file-name-nondirectory (buffer-file-name))` --- $1 ${2:-*- lexical-binding: t -*-}

;; Copyright (C) `(format-time-string "%Y")` `user-full-name`
;;
;; Author: `user-full-name` `user-mail-address`
;; Maintainer: `user-full-name` `user-mail-address`
;; Created: `(format-time-string "%F")`
;; Version: $3
;; Keywords: $4
;; Homepage: ${5:not distributed yet}
;; Package-Version: $3
;; Package-Requires: $6
;;

;; This file is not part of GNU Emacs.

;;; Commentary:

;; $7

;; Put this file into your load-path and the following into your ~/.emacs:
;;   (require '`(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))`)

;;; Change Log:

;; Version $(3) `(format-time-string "%F")` GuanghuiXu
;;   - ${8:Initial release}

;;; Code:

$0

(provide '`(file-name-sans-extension (file-name-nondirectory (buffer-file-name)))`)

;;; `(file-name-nondirectory (buffer-file-name))` ends here

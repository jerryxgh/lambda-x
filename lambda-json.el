;;; lambda-json.el --- json

;;; Commentary:

;;; Code:

(require 'lambda-core)

;; json-mode -------------------------------------------------------------------
(use-package json-mode
  :ensure t
  :custom
  (json-mode-indent-level 4)
  ;; :config
  )

(provide 'lambda-json)

;;; lambda-json.el ends here
